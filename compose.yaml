services:
  db:
    image: postgres
    container_name: postgres-f1-constructor
    environment:
      POSTGRES_PASSWORD: admin
    ports: 
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
